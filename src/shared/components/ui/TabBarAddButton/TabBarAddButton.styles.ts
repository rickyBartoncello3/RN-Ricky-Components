import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  root: {
    //padding: 4,
  },
  container: {
    alignItems: 'center',
    padding: 1,
    backgroundColor: 'red',
    borderRadius: 100,
  },
});
